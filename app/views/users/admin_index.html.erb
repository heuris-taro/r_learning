<% if notice %><p id="notice"><%= notice %></p><% end %>
<h1>管理者一覧</h1>
<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin.each do |admin| %>
  <tr>
    <td><%= admin.name %></td>
    <td><%= link_to 'Show', admin %></td>
    <td><%= link_to 'Edit', edit_user_path(admin) %></td>
    <td><%= link_to 'Destroy', admin, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Administrator', new_user_path(authority: "administrative_privileges") %>